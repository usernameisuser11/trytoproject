or(let i=0,j=p.length-1;i<p.length;j=i++){const a=p[i],b=p[j];if(((a[1]>y)!=(b[1]>y))&&(x<(b[0]-a[0])*(y-a[1])/(b[1]-a[1]+1e-9)+a[0]))c=!c}return c}
const img=new Image();img.src=COUNTMAP;await img.decode();const cv=document.createElement('canvas');cv.width=REF_W;cv.height=REF_H;const cx=cv.getContext('2d',{willReadFrequently:true});cx.drawImage(img,0,0);const d=cx.getImageData(0,0,REF_W,REF_H).data;let N=0;for(let i=0;i<d.length;i+=4)N+=d[i];const pos=new Float32Array(N*3),tar=new Float32Array(N*3),scat=new Float32Array(N*3),seed=new Float32Array(N),delay=new Float32Array(N),size=new Float32Array(N),edge=new Float32Array(N),region=new Float32Array(N);let o=0;function countAt(x,y){if(x<0||y<0||x>=REF_W||y>=REF_H)return 0;return d[(y*REF_W+x)*4]}for(let y=0;y<REF_H;y++)for(let x=0;x<REF_W;x++){const n=countAt(x,y);if(!n)continue;const ed=(countAt(x-1,y)===0||countAt(x+1,y)===0||countAt(x,y-1)===0||countAt(x,y+1)===0)?1:0;let rg=0;if(inside(x,y,L)||inside(x,y,R))rg=1;else if(y<82&&x>218&&x<294)rg=2;else if(y>84&&y<101&&x>244&&x<268)rg=3;for(let q=0;q<n;q++,o++){const k=o*3,s=hash(o*1.731+q*17.17),wx=(x-(REF_W-1)/2)*SCALE,wy=((REF_H-1)/2-y)*SCALE,z=(s-.5)*(rg===1?1.0:1.6);tar[k]=wx;tar[k+1]=wy;tar[k+2]=z;const a=hash(o*.731+9.2)*6.283185,r=44+hash(o*1.113+17.4)*110;scat[k]=Math.cos(a)*r;scat[k+1]=(hash(o*2.19+3.7)-.5)*120;scat[k+2]=(hash(o*3.01+1.4)-.5)*32;pos[k]=scat[k];pos[k+1]=scat[k+1];pos[k+2]=scat[k+2];seed[o]=s;edge[o]=ed;region[o]=rg;if(rg===1)delay[o]=.54+Math.min(1,Math.abs(x/(REF_W-1)-.5)*2)*.25+s*.03;else if(rg===2)delay[o]=.43+s*.10;else if(rg===3)delay[o]=.82+s*.025;else delay[o]=.10+(1-y/(REF_H-1))*.34+s*.025;size[o]=.72+n*.055+ed*.60+s*.28}}
const app=document.getElementById('app'),scene=new THREE.Scene();scene.background=new THREE.Color(0);const camera=new THREE.OrthographicCamera(-70,70,40,-40,-100,100);camera.position.z=20,renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.setSize(innerWidth,innerHeight);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.12;app.appendChild(renderer.domElement);const composer=new EffectComposer(renderer);composer.addPass(new RenderPass(scene,camera));const bloom=new UnrealBloomPass(new THREE.Vector2(innerWidth,innerHeight),1.2,.62,.15);bloom.strength=1.5;bloom.radius=.72;composer.addPass(bloom);function fit(){const w=REF_W*SCALE,h=REF_H*SCALE,a=innerWidth/innerHeight,m=1.07;let hw=w*m/2,hh=h*m/2;if(hw/hh<a)hw=hh*a;else hh=hw/a;camera.left=-hw;camera.right=hw;camera.top=hh;camera.bottom=-hh;camera.updateProjectionMatrix()}fit();const g=new THREE.BufferGeometry();g.setAttribute('position',new THREE.BufferAttribute(pos,3));g.setAttribute('aTarget',new THREE.BufferAttribute(tar,3));g.setAttribute('aScatter',new THREE.BufferAttribute(scat,3));g.setAttribute('aSeed',new THREE.BufferAttribute(seed,1));g.setAttribute('aDelay',new THREE.BufferAttribute(delay,1));g.setAttribute('aSize',new THREE.BufferAttribute(size,1));g.setAttribute('aEdge',new THREE.BufferAttribute(edge,1));g.setAttribute('aRegion',new THREE.BufferAttribute(region,1));const vs=`attribute vec3 aTarget;attribute vec3 aScatter;attribute float aSeed,aDelay,aSize,aEdge,aRegion;uniform float uTime,uProgress,uFinal;varying float vEdge,vRegion,vSeed,vAlpha;float sat(float x){return clamp(x,0.,1.);}float ease(float x){x=sat(x);return x*x*(3.-2.*x);}void main(){float f=ease((uProgress-aDelay)/max(.055,1.-aDelay));if(uFinal>.5)f=1.;vec3 p=mix(aScatter,aTarget,f);float m=(1.-f)*(1.-uFinal),a=aSeed*6.283+uTime*(.16+.13*aSeed);p.x+=cos(a)*m*(1.5+3.5*aSeed);p.y+=sin(a*1.07)*m*(1.+2.8*aSeed);p.z+=sin(uTime*.55+aSeed*40.)*m*1.4;gl_Position=projectionMatrix*modelViewMatrix*vec4(p,1.);gl_PointSize=clamp(aSize*(1.45+.35*aEdge),1.,6.5);vEdge=aEdge;vRegion=aRegion;vSeed=aSeed;vAlpha=mix(.09,1.,f);}`;const fs=`varying float vEdge,vRegion,vSeed,vAlpha;void main(){vec2 q=gl_PointCoord-.5;float d=length(q);if(d>.5)discard;float core=smoothstep(.49,.07,d),glow=smoothstep(.5,0.,d);vec3 c=vec3(.055,.003,.002);if(vRegion>.5&&vRegion<1.5)c=vec3(.12,.004,.002);if(vRegion>1.5&&vRegion<2.5)c=vec3(.55,.022,.006);if(vRegion>2.5)c=vec3(1.,.10,.02);c=mix(c,vec3(.95,.055,.012),vEdge*(.30+.40*vSeed));float a=(core*.72+glow*.28)*vAlpha*(.55+.4*vEdge);gl_FragColor=vec4(c*(.8+glow*.6),a);}`;const mat=new THREE.ShaderMaterial({uniforms:{uTime:{value:0},uProgress:{value:0},uFinal:{value:0}},vertexShader:vs,fragmentShader:fs,transparent:true,depthWrite:false,blending:THREE.AdditiveBlending});scene.add(new THREE.Points(g,mat));const eyeGeo=new THREE.BufferGeometry().setFromPoints([new THREE.Vector3((251-255.5)*SCALE,(128.5-92)*SCALE,2),new THREE.Vector3((261-255.5)*SCALE,(128.5-92)*SCALE,2)]);const eyeMat=new THREE.PointsMaterial({size:4.2,color:0xff260d,transparent:true,opacity:0,depthWrite:false,blending:THREE.AdditiveBlending});scene.add(new THREE.Points(eyeGeo,eyeMat));document.getElementById('loading').style.opacity='0';setTimeout(()=>document.getElementById('loading')?.remove(),450);const TOTAL=40;let t=0,play=true,final=false,last=performance.now();const phase=document.getElementById('phase'),bar=document.getElementById('progress'),clock=document.getElementById('clock'),flash=document.getElementById('flash'),hud=document.getElementById('hud');function pname(p){if(p<.1)return'VOID — scattered embers';if(p<.22)return'LEGS — traced coordinates lock in';if(p<.36)return'TORSO — silhouette rises';if(p<.44)return'HEAD — body resolves';if(p<.56)return'HORNS — crown forms';if(p<.70)return'WING BONES — span opens';if(p<.84)return'WINGS — reference silhouette fills';return'FINAL — exact reference coordinates'}function update(){const p=final?1:clamp(t/TOTAL);mat.uniforms.uTime.value=final?TOTAL:t;mat.uniforms.uProgress.value=p;mat.uniforms.uFinal.value=final?1:0;eyeMat.opacity=(final?1:clamp((p-.80)/.08))*.95;flash.style.opacity=String(Math.exp(-Math.pow((p-.61)/.025,2))*.26);phase.textContent=final?'FINAL FORM — exact traced target':pname(p);bar.style.width=(p*100).toFixed(1)+'%';const s=Math.floor(final?TOTAL:t);clock.textContent=`${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')} / 00:40`}function loop(now){requestAnimationFrame(loop);const dt=Math.min(.05,(now-last)/1000);last=now;if(play&&!final){t+=dt;if(t>=TOTAL)t=0}update();composer.render()}requestAnimationFrame(loop);function resetAnim(){t=0;final=false;play=true;formedBtn.textContent='Final Form';pauseBtn.textContent='Pause'}function toggle(){play=!play;pauseBtn.textContent=play?'Pause':'Play'}function setFinal(){final=!final;if(final)play=false;else{play=true;t=0}formedBtn.textContent=final?'Resume':'Final Form'}const pauseBtn=document.getElementById('pause'),restartBtn=document.getElementById('restart'),formedBtn=document.getElementById('formed');pauseBtn.onclick=toggle;restartBtn.onclick=resetAnim;formedBtn.onclick=setFinal;addEventListener('keydown',e=>{if(e.code==='Space'){e.preventDefault();toggle()}else if(e.key==='r'||e.key==='R')resetAnim();else if(e.key==='f'||e.key==='F')setFinal();else if(e.key==='h'||e.key==='H')hud.style.opacity=hud.style.opacity==='0'?'1':'0'});addEventListener('resize',()=>{renderer.setSize(innerWidth,innerHeight);composer.setSize(innerWidth,innerHeight);fit()});</script></body></html>